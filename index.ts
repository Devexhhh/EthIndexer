let CURRENT_BLOCK_NUMBER = 12313123;

async function main() {
    // get interested addresses from the DB

    // inspect the block from native eth transactions on one of these addresses

    // Bad approach => Update the balance in the database
}